# Use credentials for this new product only. This example is for local development.
DATABASE_URL=postgresql://rekky_local:local_only_change_me@127.0.0.1:55432/rekky_local
PORT=3088
HOST=127.0.0.1
GOOGLE_CLIENT_IDS=
APPLE_CLIENT_IDS=
# Required for paid processing when that feature is enabled later.
# Do not copy credentials or database URLs from the legacy application.
